BUILD_PATH="\\wsl.localhost\Ubuntu-24.04\var\www\atrocore\src\atrocore\client"
BACKEND_URL=https://atrocore.local
DEV_PORT=5173
# Where the backend serves its static files from. Relative to this folder, defaults to ../../public.
# Static files are read from disk instead of being requested from the backend.
PUBLIC_PATH=../../public
